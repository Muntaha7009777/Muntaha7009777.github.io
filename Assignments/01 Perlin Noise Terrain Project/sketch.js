// Perlin Noise Terrain
// Muntaha Chowdhury
// Oct 1, 2024
// A terrain generated by Noise with a flag/panning/average/changeable + 2D Noise

let timeAtTerrain = 5;

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
  generateTerrain();
}

function generateTerrain() {
  terHeight = noise(timeAtTerrain);
  terHeight = map(terHeight, 0, 1, 10, height-10);
  for (let block = 0; block < width; )
}

